machine:
  node:
    version: v8.5.0
  yarn:
    version: 1.1.0
  ruby:
    version: 2.3.0

dependencies:
  pre:
    - rm -rf ~/.yarn
    - npm install -g yarn
    - yarn -v
  post:
    - bundle exec rake assets:precompile

test:
  post:
    - bundle exec rubocop
    - cd client && yarn lint
