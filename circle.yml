machine:
  node:
    version: v8.9.0
  yarn:
    version: 1.3.2
  ruby:
    version: 2.3.0

dependencies:
  pre:
    - rm -rf ~/.yarn
    - npm install -g yarn
    - yarn -v
  post:
    - bundle exec rake assets:precompile

test:
  post:
    - bundle exec rubocop
    - cd client && yarn lint
