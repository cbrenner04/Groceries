machine:
  node:
    version: v9.8.0
  yarn:
    version: 1.5.1
  ruby:
    version: 2.3.0

dependencies:
  pre:
    - rm -rf ~/.yarn
    - npm install -g yarn
    - yarn -v
  post:
    - bundle exec rake assets:precompile

test:
  post:
    - bundle exec rubocop
    - cd client && yarn lint
